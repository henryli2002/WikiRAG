# 工业级半自动化 RAG 评测全景框架与落地 SOP

## 第一部分：全景架构 (动静分离，分层验证)

整个框架遵循“先验证地基，再验证上层”的第一性原理，分为三个递进阶段。

### 阶段一：纯检索能力的确定性验证 (Retrieval Baseline)
* **核心目标：** 验证多路召回 (如 PGVector + BM25) 与重排 (如 BGE-M3) 的纯物理寻址能力，剔除任何生成模型的干扰。
* **评测指标：** `Hit Rate @ K` (召回率下限), `MRR` (重排精准度)。
* **工作流：** 脚本批量生成打底数据 -> 人工介入修改并注入对抗陷阱 -> 脚本静默批量检索 -> 纯数学算分 -> 人工抽检 Bad Case 进行归因与架构调优。

### 阶段二：生成质量的“人机对齐”验证 (Generation Quality)
* **核心目标：** 在确保召回正确的前提下，验证大模型是否严格遵循指令，输出了无幻觉、高相关的业务答案。
* **评测指标：** `Faithfulness` (忠实度), `Answer Relevance` (相关性)。
* **工作流：** 脚本调用强模型 (如 Gemini 3.1 Pro) 作为裁判 (LLM-as-a-Judge) 进行批量打分 -> **人工必须抽检 10% 对齐基准** -> 依据荒谬的扣分 Case 修正裁判的 Prompt Criteria (评分准则)，直至大模型的打分逻辑与人类专家一致。

### 阶段三：全链路系统压力与成本审计 (System & Cost Auditing)
* **核心目标：** 验证架构的高可用性、并发承载力与商业化 API 的实际成本消耗。
* **评测指标：** `Latency` (检索延迟/首字延迟), `Token Cost` (单次 Query 成本及隐性消耗)。
* **工作流：** 编写自动化压测脚本模拟并发请求 -> 记录全链路耗时与 Token 开销 -> 人工审查极值 (如耗时毛刺、本地小模型显存溢出) -> 决策是否裁剪架构冗余 (如 Query 重写层) 或调整并发数。

---

## 第二部分：阶段一“召回质量”详细落地 SOP (生死线验证)

必须明确区分“机器算力”与“人工心力”的边界。

### 动作 1：构建带真值的“黄金测试集” (Golden Dataset)
不要使用业务系统真实但无法定位标答的 Query，必须逆向构建以计算绝对准确率。

1.  **脚本 API 粗加工 (打底)：**
    * 从 Wiki 知识库中，依据时间、分类或随机策略，抽取 200 个文本质量高、长度适中的 Chunk。
    * 调用大模型 API 遍历这 200 个 Chunk，要求大模型基于每个 Chunk 逆向生成 1 个对应的问题。
    * 形成基础数据表结构：`[query_id, expected_chunk_id, generated_query]`。
2.  **人工精加工与对抗注入 (核心屏障)：**
    * 大模型生成的问题通常过于紧贴原文。人工必须审视这 200 条数据，对其中至少 30% 进行**对抗性改造**：
        * **注入同义词壁垒：** 将原文的“计算机视觉”改为提问中的“图像识别技术”（测试 Embedding 的语义映射空间）。
        * **注入信息缺失：** 刻意删掉提问中的关键定语（测试系统面对模糊意图时的召回冗余度）。
        * **注入实体歧义：** 将通用代词替换为易混淆的实体名（测试 Reranker 的精准度）。
    * 将人工修改后的数据固化，作为后续不可篡改的“基线标尺”。

### 动作 2：执行静默检索与绝对算分
切断所有 LLM 的生成动作，只验证物理寻址。

1.  **脱水运行：**
    * 编写测试脚本读取“黄金测试集”。
    * 遍历 Query 并发送给系统的检索模块。
    * 仅记录每个 Query 检索回来的 `Top-10 Chunk ID` 列表，丢弃具体文本内容。
2.  **严格算分 (纯数学验证)：**
    * 脚本对比检索返回的 ID 列表与人工标注的正确 `expected_chunk_id`。
    * 计算核心指标：
        * `Hit@1`: 正确的 Chunk 是否排在绝对第一。
        * `Hit@5`: 正确的 Chunk 是否在全五名（送给大模型组装 Context 的物理底线）。
        * `MRR@10`: 综合反映正确答案排名的倒数平均值。

### 动作 3：Bad Case 尸检与归因调优 (决定重构方向)
当基线成绩算出后 (如 Hit@5 仅为 65%)，算分结束，必须通过人工尸检决定架构演进方向。使用脚本打印出所有未命中目标（Target Chunk 未进 Top-5）的 Query 及其实际召回的错误 Top-5 文本。

**人工归因分类与应对决策：**
1.  **死因 A (词汇鸿沟)：** 用户提问词汇在目标 Wiki 中完全未出现，Embedding 模型也未能将其映射到相近向量空间。
    * *决策策略：* 必须在前置链路引入 Query 重写 (Query Rewriting)，或考虑更换/微调对垂直领域更敏感的 Embedding 模型。
2.  **死因 B (切块碎裂)：** 目标信息被生硬地切断在两个相邻的 Chunk 中，导致单个 Chunk 的匹配度得分不足以挤进 Top-5。
    * *决策策略：* 修改文本清洗与切分策略（如扩大 Chunk Size 并增加 Overlap），或开始规划引入父子块 (Parent-Child) 架构。
3.  **死因 C (排序倒挂 - Reranker 闯祸)：** 观察多路召回的原始得分，发现目标 Chunk 在初筛时进了前 10，但被 BGE-M3 Reranker 强行踩到了第 50 名外。
    * *决策策略：* 判定当前 Reranker 交叉注意力机制对业务语境存在严重误判。应立即降低 Rerank 阶段权重，调整 RRF 的融合 $K$ 值，或直接放弃通用重排模型，积累数据进行微调。